name: Build & Release Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build Executable (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            asset_name: Jal-Lijiye-Windows
          - os: macos-latest
            asset_name: Jal-Lijiye-macOS

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Build Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyQt6 Pillow pyinstaller

      - name: Build with PyInstaller
        run: |
          pyinstaller --noconfirm "Jal Lijiye.spec"

      - name: Zip Windows Artifact
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          Compress-Archive -Path "dist/Jal Lijiye" -DestinationPath "${{ matrix.asset_name }}.zip"

      - name: Zip macOS App
        if: runner.os == 'macOS'
        run: |
          cd dist && zip -r "../${{ matrix.asset_name }}.zip" "Jal Lijiye.app"

      - name: Upload Workflow Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: ${{ matrix.asset_name }}.zip

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ${{ matrix.asset_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
